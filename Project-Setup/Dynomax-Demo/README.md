# Dynomax Demo Project

This project exists only to certify the generic project/action/workflow pipeline.

It uses the public ATX home page as configured test data but does not load the ATX Solutions project, credentials, source contracts or business workflows.

Run:

```text
Project-Workflow\Sessions\000001-ATX-Public-Homepage-Observation\Run.bat
```
