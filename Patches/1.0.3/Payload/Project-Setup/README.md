# Dynomax Projects

Each direct subfolder represents one independently configured testing project.

A project owns its target URLs, environments, credentials references, action library and workflow sessions. Dynomax Core does not select a project until a project action or workflow BAT is explicitly run.

Use `_Template` when creating another project.
