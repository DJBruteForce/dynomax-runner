# ATX Solutions Dynomax Project

This folder is an optional Dynomax project definition. ATX URLs, actions and workflows must remain inside this folder.

Run a workflow from:

```text
Project-Workflow\Sessions\<Session>\Run.bat
```

The root Dynomax framework self-test does not load or contact ATX.
